开发新功能：$ARGUMENTS

步骤：
1. 先分析需求，列出要修改的文件
2. 如果涉及数据库变更，先修改 db/database.js 的初始化函数
3. 后端API写在对应的路由文件中
4. 前端逻辑写在对应的JS文件中（不要写进index.html）
5. 完成后 npm start 验证
6. 按照 web-testing Skill 执行端到端测试：
   - 从用户视角操作完整流程
   - 不能只看API 200就算通过，必须确认用户最终看到了正确结果
   - 设置30秒超时阈值
   - 至少重复验证3次确认稳定性
7. 告诉我改了哪些文件，新增了什么API，测试结果（PASS/FAIL）
